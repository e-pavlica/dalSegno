<script type="text/javascript" src="https://maps.googleapis.com/maps/api/js?key=AIzaSyB-r1YQPElxrGtWhUsfpi8lI18NUkMss1w&sensor=false"></script>

:javascript
  function initialize() {
    
    var mapOptions = {
      center: new google.maps.LatLng(-34.397, -115.644),
      zoom: 8,
      mapTypeId: google.maps.MapTypeId.ROADMAP
    };
    var map = new google.maps.Map(document.getElementById("map-canvas"),
        mapOptions);

  function codeAddress() {
    var address = document.getElementById('address').value;
    geocoder.geocode( { 'address': address}, function(results, status) {
      if (status == google.maps.GeocoderStatus.OK) {
        map.setCenter(results[0].geometry.location);
        var marker = new google.maps.Marker({
            map: map,
            position: results[0].geometry.location
        });
      } else {
        alert('Geocode was not successful for the following reason: ' + status);
      }
    });
  }
  google.maps.event.addDomListener(window, 'load', initialize);
  }



.pure-g-r
  .pure-u-1.card
    = image_tag @user.gravatar_url, class: "profileImg"
    %h2=@user.name
    .pure-g-r
      .pure-u-1
        %h4 Contact Info
      .pure-u-1-2
        %i{class:"icon-email"}
        =@user.email
        %br
        %i{class:"icon-ios7-telephone"}
        =@user.phone
        %br
        =link_to "edit", edit_user_path , class: "pure-button editButton"
      .pure-u-1-2
        %i{class:"icon-location"}
        .address
          =@user.address
          %br
          =@user.city + ", " + @user.state + " " + @user.zip
        #map-canvas


        